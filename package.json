{
  "name": "orbis-langchain-agent",
  "version": "1.0.0",
  "description": "AI agent with access to 8,000+ APIs via Orbis — pay-per-call with USDC on Base",
  "private": true,
  "type": "module",
  "scripts": {
    "agent": "tsx agent.ts",
    "dev": "tsx --watch agent.ts"
  },
  "dependencies": {
    "@langchain/core": "^0.3.0",
    "@langchain/openai": "^0.3.0",
    "@x402/fetch": "^2.14.0",
    "langchain": "^0.3.0",
    "orbis-langchain": "^0.1.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
